# -*- coding: utf-8 -*-
"""S2Pref live 模式的 prompt 模板（可选）。

仅在配置了 OpenAI 兼容端点时使用。对应论文的三个任务理念：
上下文对齐、冲突检测与澄清、情境偏好推断。
"""

CONTEXT_ALIGN_PROMPT = """你是一个具备『情境偏好感知』能力的助手。
用户拥有两类偏好：
- 稳定偏好（与语境无关，始终成立）：{stable}
- 情境偏好（随语境翻转）：在「{ctx}」语境下，应满足：{situ_pref}

用户请求：{user}

请给出一个个性化回复：保持稳定偏好，并按当前语境正确触发情境偏好。控制在 2~3 句。"""

CONFLICT_PROMPT = """用户拥有一个『情境偏好』，它在不同语境下会冲突：
{aspect}：语境A「{ctx_a}」→{pref_a}；语境B「{ctx_b}」→{pref_b}。

用户请求：{user}

该请求未指明语境，存在潜在偏好冲突。请不要盲目假设，而是主动、自然地向用户澄清语境（1~2 句）。"""
